(function( ng, app, us ) {

	"use strict";

	/* Application Controller */
	app.controller('ApplicationController', [
				 '$scope',
			function(
				 $scope
		) {

		
	}]);
})( angular, SFApplication, _ );